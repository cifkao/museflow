language: python
python:
  - "3.6"
jobs:
  include:
    - name: 'Model tests'
      install:
        - pip install -e .
      script:
        - cd tests; ./test_models.sh
    - name: 'Pylint'
      install:
        - pip install -e .
        - pip install pylint
      script:
        - pylint museflow
    - name: 'Code style'
      install:
        - pip install pycodestyle pydocstyle
      script:
        - pycodestyle museflow
        - pydocstyle museflow
